
# third_party headers

필요 시 아래 단일 헤더를 이 폴더에 추가하세요.

- HTTP: cpp-httplib (`httplib.h`)
  - https://github.com/yhirose/cpp-httplib
- JSON: nlohmann/json (`json.hpp`)
  - https://github.com/nlohmann/json

추가 후 `#include "third_party/httplib.h"`, `#include "third_party/json.hpp"` 식으로 사용 가능합니다.
